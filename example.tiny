function fact_it(n)
	local a = 1
	local i = 2
	while i <= n do
		a = a * i
		i = i + 1
	end
	a
end

function fact_rec(n)
	if n == 0 then 1
	else n * fact_rec(n-1) end
end

print(fact_it(10) == fact_rec(10))
